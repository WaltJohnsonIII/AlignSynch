# Cursor Agent Configuration
# Use "auto" model selection to prevent quota issues

## Model Selection
- Use "auto" model selection for all agents
- This prevents quota exhaustion on specific models
- Let Cursor's auto-selection handle model choice optimally
